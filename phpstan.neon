includes:
    - vendor/phpstan/phpstan/conf/config.level7.neon

parameters:
    inferPrivatePropertyTypeFromConstructor: true
    ignoreErrors:
        - '{Instanceof between .*Token.* will always}'
        
    paths:
        - lib

